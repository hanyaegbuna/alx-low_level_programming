#include <stdio.h>
#include <string.h>
#include <main.h>

/**
 * main - print the name of the program
 * @argc: count arguments
 * @argv: Arguments
 */

int main(int argc, char ** argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
